name: BFF Project CICD

on:
  push:
  pull_request:
  workflow_dispatch:

permissions:
  contents: write
  packages: write

jobs:
  CICD:
    uses: mairie360/CICD/.github/workflows/BFFs-cicd.yml@v1.14.1
    with:
      package_name: bff-project
      cicd_version: v1.14.1
      node_version: "22"
      openapi_spec_path: openapi.json
    secrets: inherit
